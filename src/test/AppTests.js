const assert = require('chai').assert;
const app = require('../utility');

describe('Util',function() {
    it('should return the bill', function generateBill() {
        assert.equal(generateBill(),100);
    });
    // it("Should return string type",function() {
    //     let result = 
    // });
});